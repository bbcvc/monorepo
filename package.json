{
  "name": "monorepo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm -F ui run dev & pnpm -F web run dev",
    "dev:web": "pnpm -F web run dev",
    "dev:ui": "pnpm -F ui run dev",
    "dev:icons": "pnpm -F icons-lib run dev",
    "format": "prettier **/*.{js,ts,jsx,tsx} --write --ignore-path .gitignore && git update-index --again",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.2.1"
  },
  "dependencies": {
    "tslib": "^2.3.0"
  }
}
